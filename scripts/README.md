# Scripts

- `run_scifact_diagnostic.sh`: quick truncated SciFact run with TF-IDF-lite.
- `run_scifact_bge.sh`: full SciFact run with the BGE sentence-transformer encoder.

Generated outputs are written to `reports/`.
